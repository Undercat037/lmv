[package]
name = "lmv"
version = "1.3.0"
edition = "2024"
license = "GPL-3.0"

[dependencies]
clap = { version = "4.4", features = ["derive"] }
clap_complete = "4.4"
walkdir = "2"

[profile.release]
opt-level = 3
lto = true